{
  "name": "facebook-messenger-ai-starter",
  "version": "1.1.0",
  "private": true,
  "description": "Chatbot AI cho Facebook Messenger, triển khai trực tiếp từ GitHub lên Render.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.js",
    "start": "node src/server.js",
    "knowledge": "node --env-file=.env scripts/upload-knowledge.js",
    "profile": "node --env-file=.env scripts/setup-messenger-profile.js",
    "check": "node scripts/check-code.js",
    "test": "npm run check"
  },
  "dependencies": {
    "express": "^5.1.0",
    "openai": "^7.3.0"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
